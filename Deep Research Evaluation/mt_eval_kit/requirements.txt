# Core (always required)
pydantic>=2.6
pyyaml>=6.0
tenacity>=8.2

# Judge SDK — install only the one you use:
#   openai>=1.40              # OpenAI API or any OpenAI-compatible gateway (LiteLLM, vLLM, Ollama /v1)
#   anthropic>=0.39           # Claude
#   google-generativeai>=0.7  # Gemini
#   httpx>=0.27               # native Ollama
# The 'deterministic' judge needs none of these.

# Testing (optional)
#   pytest>=8.0
#   pytest-asyncio>=0.23
